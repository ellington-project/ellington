language: rust
rust: 
  - stable
  - beta
  - nightly 
script:
  - cargo test --verbose --features data-dependent-tests
matrix: 
  allow_failures:
    - rust: nightly
  fast_finish: true
cache: 
  directories: 
    - data